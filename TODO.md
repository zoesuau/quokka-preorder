# 鼠購易後續開發代辦

## 已完成（2026-07-29）

- 本次不提供顧客匯款回報頁，也不讓新訂單進入藍色「待確認訂金」；既有藍色資料僅保留相容顯示。
- 後台訂單統一使用「調整訂單」入口，並以「顧客變更／管理修正／韓國現場缺貨」區分原因。
- LINE 人工核對警示已改用企業綠 `#06C755`。
- LINE 警示在粉紅色「待收訂金」卡片直接顯示「訂金已入帳／尚未入帳」兩個按鈕，並依25小時規則標記已查看或立即逾期取消。
- 一般訂單狀態選單不再顯示「待確認訂金（顧客回報）」選項。
- 訂單金額變動通知已統一欄位名稱、順序與計算方式；溢付另列待退款。

## LINE OA／Postback（本次不開發）

- 後台目前維持管理登入碼；下週新增「綁定我的 LINE」流程，讓已登入管理員一鍵綁定經後端驗證的 LINE User ID，之後可直接使用 LINE 登入，不要求商家自行查找或輸入 User ID。
- 管理員綁定介面只顯示姓名與頭像，並提供解除綁定、第二位備援管理員與店主緊急登入碼流程；不得建立跨商家共用的萬用密碼。
- 建立「我的訂單」查詢入口，回傳顧客目前最新的訂單卡片。
- 讓顧客從多筆訂單中選擇要處理的訂單。
- 以 Postback 明確帶入訂單編號，支援匯款資訊、聯絡客服與訂單問題分類。
- 建立客服待辦、處理狀態與逾時提醒。
- 評估會員／CRM 標籤、熟客辨識與個人化 Rich Menu。
- 評估 AI 問題分類及銀行文字／圖片 OCR；只提供人工核對建議，不自動確認付款。
- 將唯一的 LINE Webhook URL 維持為中央事件中樞，未來由中樞分流至訂單、客服、CRM 與分析模組。

## 交付與付款多方案（未來升級，本次不開發）

- 第一位新客人的本次範圍維持「單店單方案」：固定使用 7-11 店到店並一次支付全款；現有店家則維持 iOPEN Mall 與訂金／尾款流程。
- 共用母版以訂單流程模式區分客戶設定，不為不同客戶複製永久分叉的程式；每張訂單成立時鎖定當時的付款與交付方案，日後設定異動不得改變舊訂單。
- 未來有實際需求時，再讓賣家同時開放多個「交付與付款方案」，由顧客下單時選擇；不要把 iOPEN Mall 當成單純物流，必須連同訂金、尾款及賣場流程一起建模。
- 前台依顧客選擇的方案顯示對應必填欄位與本次應付金額；若只開放一個方案，直接套用，不增加多餘選擇步驟。
- 後端必須驗證該方案仍為新訂單允許使用的選項，並依訂單快照處理金額、狀態、退款、出貨、LINE 通知及匯出，不得依賣家目前設定回推舊訂單。
- 關閉某個方案只能阻止新訂單使用，既有訂單仍須保留原流程並可在後台完成處理。
- 多方案正式開發前，先確認各方案的運費、免運、訂金比例、尾款管道、付款期限、必填取件資料與取消退款規則，再建立組合測試矩陣。

## 訂閱停權（下週開發，本次不處理）

- 新增只有金鶴賣平台方能控制的服務狀態，例如 `active`、`suspended`；不可與賣家自行操作的「前台停賣」共用。
- `suspended` 必須由 GAS 後端強制阻止新訂單、商品與訂單異動、通知發送及排程副作用，不能只隱藏前台按鈕。
- 暫停畫面需清楚顯示服務已暫停；恢復訂閱後可切回 `active`，不得破壞既有訂單與資料。
- 規劃取消訂閱後的唯讀／CSV 匯出期限、資料保存期限、正式終止與重新啟用流程。
- 客戶換系統時，先完成資料匯出與新系統 OA 串接，再停用金鶴賣 Webhook；不得以刪除 OA、Provider 或扣留 OA 權限作為停權方式。
- 增加前端、GAS API、排程、LINE 通知、直接呼叫 API 與恢復訂閱的回歸測試。

## 原則

- OA 負責顧客互動與通知，後台負責管理與決策。
- 自由文字、圖片或檔案只能產生「待人工核對」訊號，不得直接判定已付款。
- 所有舊卡片操作都必須由後端重新檢查訂單最新狀態。
