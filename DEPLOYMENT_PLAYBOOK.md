# 鼠購易版本與部署手冊

本文件規範鼠購易母版、新功能、客戶設定、版本標記、正式部署與回滾方式。目標是在維持單一產品母版的前提下，讓每位客戶的實際運行版本都能被追蹤與重現。

## 核心原則

> **共用功能在 `feature/*` 開發並合併至 `main`；`main` 以版本 tag 發布；客戶差異由設定與功能開關管理。**

- `main` 是唯一產品母版，必須維持已測試、可部署狀態。
- 不直接在 `main` 開發一般功能。
- 不因客戶不同而複製或刪除共用程式。
- 客戶部署不會自動跟隨每次母版更新；必須經過選版、測試與驗收。
- 部署成功不代表功能驗收成功，正式環境仍須執行安全的端對端檢查。
- 每次部署都要保留明確的前一版本與回滾路徑。

## 分支規則

### `main`

`main` 代表目前最新、已測試通過的產品母版。

共同功能完成後才可合併回 `main`。任何要部署的產品版本都必須能追溯到 `main` 上的確切 commit。

### `feature/*`

新功能從最新 `main` 建立短期功能分支：

```text
feature/payment
feature/logistics
feature/preorder
feature/inventory-lock
```

標準流程：

```text
main
↓
建立 feature 分支
↓
開發與測試
↓
驗收通過
↓
合併回 main
```

功能分支完成後不作為正式部署來源，也不長期保留客戶設定。

### `hotfix/*`

正式環境的緊急修正使用短期 `hotfix/*` 分支，修正與回歸測試完成後合併回 `main`，再建立新的修正版 tag。

```text
hotfix/order-timeout
hotfix/payment-callback
```

不得只在客戶部署分支修正共用 Bug，否則其他客戶仍會保留相同問題。

### `deploy/*`（過渡期）

在部署自動化完成前，可以暫時使用：

```text
deploy/echo
deploy/customer-b
deploy/customer-c
```

部署分支只代表該客戶當下要部署的產品版本與非機密設定，不是新的產品母版。

部署分支必須遵守：

1. 從已標記版本的 `main` 建立或更新。
2. 只允許客戶設定、品牌資源與極少量真正無法共用的修改。
3. 不在部署分支開發共用功能。
4. 不將部署分支合併回 `main`。
5. 可共用的客戶需求必須回到 `feature/*` 開發。
6. 每次部署都記錄產品 tag、commit SHA 與實際部署識別碼。

當部署工具能依客戶設定產生部署成品後，逐步停止使用長期 `deploy/*` 分支。

## 功能開關

不同客戶是否啟用某項功能，應由設定控制：

```js
const FEATURES = {
  payment: false,
  logistics: false,
  preorder: true,
  inventory: true,
  multipleAdmins: false,
};
```

功能開關必須同時作用於：

- 前端畫面與操作入口。
- GAS API 權限與業務規則。
- 排程、通知及資料寫入等副作用。
- 該客戶的自動測試與部署驗收。

不能只隱藏前端按鈕，後端仍接受未啟用功能的請求。

## 客戶設定與機密資料

### 可納入客戶設定

- 品牌名稱。
- Logo 與品牌資源。
- 前端 GAS Web App URL。
- LIFF ID。
- 功能開關。
- 不具機密性的顯示設定。

### 不可提交到 Git

- LINE Messaging Access Token。
- Webhook Forwarding Secret。
- 管理員登入碼。
- 其他 API Secret 或私人金鑰。

機密資料必須放在各客戶獨立 GAS 專案的 Script Properties 或相應的安全環境設定中。

`SPREADSHEET_ID`、LINE Channel 設定與 GAS URL 必須依實際用途區分前端公開設定與後端設定，不得因方便而將後端機密送到瀏覽器。

## 版本標記

穩定的產品母版使用語意化版本：

| 版本 | 用途 |
| --- | --- |
| `v1.0.0` | 第一個正式穩定版 |
| `v1.1.0` | 向下相容的新功能 |
| `v1.1.1` | 向下相容的 Bug 修正 |
| `v2.0.0` | 不相容更新或重大架構變更 |

例如：

```text
v1.0.0 基本訂單系統
v1.1.0 加入防重送
v1.2.0 加入預購
v1.3.0 加入金流
v1.3.1 修正金流回傳錯誤
```

產品 tag 只描述母版能力。客戶實際使用哪個版本，另外寫入部署紀錄，不用客戶名稱取代產品版本。

## 部署紀錄

每次正式部署至少記錄：

```text
客戶：Echo
產品版本：v1.2.0
Git Commit：<commit SHA>
部署日期：2026-08-01
部署人員：Zoe

啟用功能：
- preorder: true
- payment: false
- logistics: false

前端網址：<URL>
GAS Deployment ID：<ID>
GAS Version：<版本>
LIFF ID：<ID>
試算表代稱：<非機密識別名稱>

測試結果：通過／未通過
正式驗收：通過／未通過
上一個可回滾版本：v1.1.1
備註：<本次差異或已知限制>
```

部署紀錄不得包含 Token、密碼或 Secret。

## Echo 首次正式部署流程

```text
1. 同步並確認本機 main 與遠端 main
2. 從 main 建立功能或上線修正分支
3. 完成 Echo 上線前需要的修正
4. 執行語法檢查、自動測試與必要的視覺驗收
5. 將共用修正合併回 main
6. 在 main 建立產品版本 tag
7. 套用 Echo 的非機密設定與功能開關
8. 部署到獨立測試 GAS 與測試試算表
9. 驗證完整下單、防重送、庫存、LINE 通知與後台流程
10. 記錄測試環境結果與確切 commit
11. 部署正式 GAS 與前端
12. 對正式環境執行安全的 smoke test
13. 記錄正式 Deployment ID、版本與回滾目標
```

若上線前修正屬於鼠購易共用能力，必須合併回 `main`，不能只留在 `deploy/echo`。

## 部署驗收最低要求

- 測試的是準備部署的確切 commit 與客戶設定。
- 前端與 GAS 後端版本相容。
- 使用獨立測試試算表，不寫入正式訂單或庫存。
- 正常下單成功且訂單資料正確。
- 重複請求不會建立重複訂單或重複扣庫存。
- 庫存不足、停賣、登入失效與後端錯誤有明確結果。
- LINE 顧客通知及管理端通知符合啟用設定。
- 正式網址載入的是本次部署版本，不是舊快取。
- 部署後完成一輪安全的正式 smoke test。

## 回滾規則

部署前必須先記錄：

- 前一個產品 tag 與 commit SHA。
- 前一個前端部署版本。
- 前一個 GAS Version 與 Deployment ID。
- 是否包含試算表欄位或資料遷移。

若正式驗收失敗：

1. 視風險暫停收單或關閉受影響功能。
2. 將 GAS deployment 切回前一個已驗證版本。
3. 將前端切回相容的前一版本。
4. 確認前後端版本一致。
5. 執行回滾後 smoke test。
6. 記錄失敗原因、影響範圍與回滾結果。
7. 將缺陷轉成永久回歸測試後，再重新發布修正版。

若部署包含不可逆的資料或試算表結構變更，必須在部署前另行建立備份與遷移計畫，不能只依靠程式版本回滾。

## 未來部署自動化方向

部署自動化完成後，以客戶設定清單取代長期部署分支：

```text
main
├─ deployments/echo.json
├─ deployments/customer-b.json
└─ deployments/customer-c.json
```

部署工具依指定客戶與產品版本組合成品：

```text
產品 tag
+
客戶非機密設定
+
功能開關
↓
測試
↓
部署
↓
產生部署紀錄
```

在有實際客戶數與部署頻率證明需要之前，不為此提前導入複雜平台。
