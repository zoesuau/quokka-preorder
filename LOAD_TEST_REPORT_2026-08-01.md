# 訂單併發安全測試報告（2026-08-01）

## 結論

原正式流程未通過 10／20 筆測試。修正版已在 `feature/load-testing` 與 GAS 測試部署第 6 版通過隔離驗證，並於 2026-08-01 依序部署正式前端與正式 GAS 第 35 版。

- 足量庫存：5／5、10／10、20／20 全部成功，沒有漏單或重複編號。
- 最後一件：20 筆同時請求只有 1 筆建立成功，其餘 19 筆明確回傳 `OUT_OF_STOCK`。
- 相同 requestId：兩筆同時請求回傳同一訂單編號，其中一筆 `duplicate:true`，工作表只有一列。
- 正式前端會在 sessionStorage 保存 requestId；相同 payload 重送會沿用同一識別碼。
- 有限庫存會在 Script Lock 內重新檢查與扣減，扣到 0 時同步下架；未設定庫存的舊商品維持原行為。
- 寫入訂單或冪等紀錄失敗時，會回滾已扣庫存及新增訂單。
- 取消訂單會回補有限庫存；重複取消不會再加一次，取消寫入失敗會回滾庫存。

本報告的正式流程模擬只寫入私有測試試算表的 `正式流程模擬訂單`，不會建立正式訂單、不會修改商品或庫存，也不會發送 LINE。

## 修正前實測結果（第 4 版）

| 同時請求 | 成功 | 失敗 | 最快 | 最慢 | 平均 | 模擬超賣風險 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 5 | 5 | 0 | 5.409 秒 | 25.699 秒 | 14.931 秒 | 4 筆 |
| 10 | 5 | 5 | 5.706 秒 | 25.703 秒 | 18.640 秒 | 4 筆 |
| 20 | 5 | 15 | 6.579 秒 | 28.691 秒 | 20.141 秒 | 4 筆 |

所有失敗請求都有 HTTP 回應，後端回傳 `success:false` 與 `SERVER_ERROR`，沒有被畫面誤判為成功。失敗時間與目前正式流程的 `ScriptLock.waitLock(20000)` 相符，鎖等待逾時是主要風險；在改動正式流程前仍應由 GAS 執行記錄確認最終例外文字。

## 修正後實測結果（第 5、6 版）

| 同時請求 | 模擬庫存 | 成功 | 失敗 | 最快 | 最慢 | 平均 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 5 | 5 | 5 | 0 | 7.951 秒 | 13.390 秒 | 10.606 秒 |
| 10 | 10 | 10 | 0 | 10.173 秒 | 22.408 秒 | 16.067 秒 |
| 20 | 20 | 20 | 0 | 15.495 秒 | 41.437 秒 | 27.594 秒 |
| 20 | 1 | 1 | 19（`OUT_OF_STOCK`） | 18.220 秒 | 42.853 秒 | 29.424 秒 |

20 筆皆可取得明確結果，但最慢約 41–43 秒；這表示資料正確性已通過，使用者等待時間仍需在正式上線後監控。

第 6 版加入取消回庫後再次執行 20 筆回歸測試：足量庫存 20／20 成功；最後一件 1 筆成功、19 筆明確回 `OUT_OF_STOCK`；相同 requestId 同送兩次只寫入一列。正式資料雜湊及 LINE 計數仍完全不變。

## 正式資料安全核對

三組測試的核對結果一致：

- `Preorders`：21 列，測試前後雜湊相同。
- `Products`：39 列，測試前後雜湊相同。
- 正式訂單新增：0。
- 正式庫存異動：0。
- LINE 通知：0。
- 重複 `testRequestId`：0。
- 重複正式訂單編號候選值：0。
- 測試資料欄位不完整：0。

## 測試環境

- GAS 測試部署：第 6 版；正式 GAS：第 35 版。
- 測試資料只位於未公開的隔離試算表。
- 測試完成後已將 `ENABLE_STRESS_TEST_MODE` 設回 `false`。
- 關閉後重新呼叫安全握手，後端回傳 `STRESS_TEST_DISABLED`。

## 正式部署紀錄

- 正式前端：Git 提交 `f7d937a`，先於 GAS 上線；線上 `storefront.js` 已確認包含 requestId 與 sessionStorage 重送識別。
- 正式 GAS：第 35 版，沿用原 Web App URL；第 34 版保留為回滾點。
- 已執行 `setupQuokkaPreorder()`，預先建立 `OrderRequestIds` 防重送索引表；執行記錄顯示完成。
- 正式唯讀健康檢查：商品目錄 `ok:true`，讀到 38 個商品。
- 正式壓測保護檢查：`formalSimulationHandshake` 回 `STRESS_TEST_DISABLED`；正式環境未設定 `ENABLE_STRESS_TEST_MODE`。
- 未建立人工正式訂單，因此沒有為部署驗證而產生 LINE 通知、扣庫存或測試顧客資料。

## 本機驗證

```bash
node tests/order-adjustment.test.js
node tests/product-inventory.test.js
node tests/public-catalog-cache.test.js
node tests/stress-test-ui.test.js
node tests/stress-test.test.js
node --check tests/run-live-formal-simulation.js
```

共 63 個自動測試通過，線上模擬執行器亦通過語法檢查。

## 已知限制與回滾

1. 取消回庫已依確認後的規則完成：有限庫存加回、無限庫存保持空白、重複取消不重複加回。
2. LINE 仍在鎖外且新訂單只呼叫一次；若程序在訂單寫入後、LINE 發送前中斷，重送會回原訂單但不補發 LINE，可能出現少數漏通知，需另行設計可追蹤通知佇列。
3. GAS 回滾：在部署管理將正式 Web App 改回第 34 版。前端回滾：將 GitHub `main` 回復到 `50791f1`。`OrderRequestIds` 可保留，不會影響第 34 版。

## 2026-08-01 資料匯出部署

- 正式前端：Git 提交 `841bc77`，GitHub Pages 已確認載入資料匯出版 CSS／JS，系統設定頁最下方顯示完整訂單與客戶資料匯出入口。
- 正式 GAS：第 39 版，沿用既有 Web App 部署 ID 與網址；第 38 版保留為回滾點。
- 部署內容：後台管理員可匯出完整訂單 CSV，以及依顧客識別資料去重後的完整客戶 CSV；不提供日期或狀態篩選。
- 權限與資料安全：匯出走既有後台管理員驗證，不公開原始試算表；CSV 已處理引號、換行與試算表公式注入風險。
- 資料遷移：無。未新增或修改正式訂單、客戶、商品、庫存與 LINE 訊息。
- 自動驗證：共 86 個測試通過，另完成 JavaScript 語法檢查與 `git diff --check`。
- 正式唯讀健康檢查：商品目錄回傳 `ok:true`，並正常回傳商品清單與系統設定。
- 回滾：GAS 可在部署管理改回第 38 版；前端可將 GitHub `main` 回復到 `7bc1e96`。

## 2026-08-01 商品封存部署

- 正式前端：Git 提交 `652b8e1`；GitHub Pages 已確認載入 `20260801-product-archive` 版資源，並包含「封存商品」與「恢復商品」操作。
- 正式 GAS：第 40 版，沿用既有 Web App 部署 ID 與網址；第 39 版保留為回滾點。
- 部署內容：商品可封存並從日常後台列表與客人頁面移除；封存資料列、既有訂單與取消訂單時的庫存回補均保留，恢復後維持下架。
- 狀態安全：封存商品不可被舊頁面的上下架操作重新上架；調整封存商品庫存不會解除封存。
- 資料遷移：無。未新增、刪除或修改正式商品、訂單、庫存與 LINE 訊息。
- 自動驗證：共 92 個測試通過，另完成 JavaScript 語法檢查與 `git diff --check`。
- 正式唯讀健康檢查：商品目錄回傳 `ok:true`，正常讀到 36 個公開商品與系統設定。
- 回滾：GAS 可在部署管理改回第 39 版；前端可將 GitHub `main` 回復到 `e5dd5ba`。

## 2026-08-02 Echo 款式個別庫存與照片部署

- 母體功能提交：`1a16088`；Echo 正式前端提交：`0905570`。
- Echo GAS：第 4 版，沿用原 Web App 部署 ID 與網址；第 3 版保留為回滾點。
- 部署內容：同一商品可選擇啟用款式個別庫存，為每個款式設定名稱、庫存與照片；前台會依款式顯示照片、售罄狀態並限制可購數量。
- 客戶設定保護：Echo 專屬 `config.js`、LIFF、GAS 網址與品牌設定未修改。
- 資料相容：`Products` 僅在後台首次完成驗證並讀取資料時附加 `variantOptionsJson` 欄位；既有商品預設繼續使用原總庫存，舊版程式會忽略新增欄位。
- 資料安全：部署驗證未建立訂單、未修改商品、庫存或顧客資料，也未發送 LINE 通知。當時公開商品目錄為空，與店家已先下架商品的狀態一致。
- 自動驗證：共 112 個測試通過，另完成 JavaScript／GAS 語法檢查、`git diff --check`、行動版視覺檢查。
- 正式唯讀健康檢查：Cloudflare 已載入 `20260802-variant-inventory`；正式 `admin.js` 與 `storefront.js` 和提交內容逐位元一致；GAS 商品目錄回傳 `ok:true`。
- 回滾：GAS 可在部署管理改回第 3 版；Echo 前端可回復到 `6501fbb`。新增空白欄位可以保留，不影響回滾版本。
