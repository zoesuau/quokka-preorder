# 鼠購易產品發展藍圖

本文件記錄鼠購易從第一套可複製產品，逐步發展為正式 SaaS 的架構方向與升級里程碑。

> **不要為了假想的 100 位客戶，今天把只有 1 位客戶的系統重寫。**

## 第一階段：現在～5 位客戶

### 架構

- GitHub Private Repository 作為母版。
- 每位客戶使用獨立的 GAS Web App。
- 每位客戶使用獨立的 Google Sheet。
- 每位客戶使用獨立的 LINE OA。
- 每位客戶使用獨立設定檔（config）。

### 功能與工程能力

- 防重送。
- 壓力測試。
- 部署流程。
- 匯出客戶資料。
- Git Branch 開發流程。

### 階段目標

完成第一套可複製的產品。

## 第二階段：5～10 位客戶

先觀察實際使用情況，不急著更換架構。

### 觀察重點

- 每筆訂單的平均執行時間。
- 是否發生 timeout。
- 是否出現配額警告。
- 是否開始出現大量客製需求。
- 是否有許多客戶同時直播。

### 新增能力

- 部署自動化。
- Feature Toggle（功能開關）。
- 更多自動測試。

## 第三階段：10～20 位客戶

若開始有許多品牌同時直播，再評估拆分執行環境或搬移下單 API。

### 方法一：拆分 Google 帳號

例如：

```text
Google A
品牌 1～5

Google B
品牌 6～10
```

這個方法成本較低，可以讓既有架構再支撐一段時間。

### 方法二：將下單 API 搬出 GAS（推薦）

例如改由 Cloudflare Worker 接收下單請求：

```text
前端
↓
Cloudflare Worker
↓
資料庫
↓
Google Sheet
```

## 第四階段：20 位以上客戶

當鼠購易已有 20 位以上活躍直播品牌，再發展為正式 SaaS。

```text
Cloudflare
↓
Database
↓
Google Sheet（報表）
↓
後台
```

此階段 Google Sheet 不再作為主資料庫，只用於報表與營運查閱。

## 客戶資料原則

目前維持：

```text
鼠購易程式
+
客戶自己的資料
```

- 提供客戶資料匯出能力。
- 不需要交付 Google Apps Script 原始碼。

## Git 與版本管理原則

- `main` 永遠是唯一母版。
- 新功能使用功能分支開發，例如 `feature/payment`。
- 功能完成並驗證後合併回 `main`。
- 各客戶部署同一套最新版程式。
- 客戶差異透過 config 與功能開關管理，不建立「05 版」、「06 版」等分裂版本。

```text
feature/payment
↓
merge
↓
main
↓
依客戶 config 部署
```

## 功能開關原則

例如：

```js
{
  payment: false,
  logistics: false,
  preorder: true
}
```

所有客戶共用同一套程式，由設定決定啟用哪些功能。

## 現階段不採用

目前不需要為尚未發生的規模問題導入：

- PostgreSQL。
- Kubernetes。
- Docker。
- 微服務。
- Redis。
- RabbitMQ。

只有在實際用量、限制與營運需求證明有必要時，才升級架構。

## 架構檢討里程碑

### 里程碑一：5 位正式付費客戶

重新檢討目前架構與實際瓶頸。

### 里程碑二：10 位客戶

開始評估並拆分執行環境。

### 里程碑三：20 位活躍直播品牌

開始建置正式 SaaS 架構。

## 每次里程碑的檢查問題

- 是否真的遇到 GAS 限制？
- 是否真的需要資料庫？
- 是否真的需要將下單 API 搬到 Cloudflare Worker？
- 問題來自系統規模，還是單一客戶的客製需求？
- 升級架構能否解決已被量測證實的問題？

未達里程碑前，優先完成客戶需要、可複製、可驗證的產品能力，不因假設性的未來規模提前重寫系統。
